package data.scripts.timelineevents;

import data.scripts.models.BaseFactionTimelineEvent;

public class ParadiseColonizationEvent extends BaseFactionTimelineEvent {

    public ParadiseColonizationEvent(int cycle, int day, int month,String token) {
        super(cycle, day, month);
    }
}
